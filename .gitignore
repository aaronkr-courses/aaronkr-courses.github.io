_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/

# Attendance app — real Firebase credentials (never commit these)
attend/firebase-config.js

# Image compression backups
scripts/image-originals/
